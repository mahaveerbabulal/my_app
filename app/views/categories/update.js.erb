$('#li_<%= @category.id %>').html('<%= @category.name %><%= link_to "X", category_path(@category), method: :delete, date: {confirm: "Are you sure?"}, remote: true %>');
$('#edit_category_<%= @category.id %>').hide();
$('#add_category').show();
